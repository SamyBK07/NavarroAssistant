package com.navarro.actions

class CalendarAction : BaseAction() {
    override fun execute(command: String): String {
        // TODO: Intégration avec le calendrier du téléphone
        return "Action calendrier pour: $command (non implémentée)"
    }
}
