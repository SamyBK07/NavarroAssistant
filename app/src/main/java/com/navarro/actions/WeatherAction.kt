package com.navarro.actions

class WeatherAction : BaseAction() {
    override fun execute(command: String): String {
        // TODO: Intégration avec une API météo ou service local
        return "Action météo pour: $command (non implémentée)"
    }
}
