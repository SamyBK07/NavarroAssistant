<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.navarro">  <!-- Assure-toi que le package est correct -->

    <!-- Permissions nécessaires -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Pour Android 12+ : Autoriser la notification des services en avant-plan -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <application
        android:name=".core.App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Activity principale -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">  <!-- Évite les duplications d'activité -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Service Hotword (Vosk) -->
        <service
            android:name=".hotword.HotwordService"
            android:foregroundServiceType="microphone"
            android:exported="false"
            android:stopWithTask="false" />  <!-- Permet au service de continuer même si l'activité est détruite -->

        <!-- Service Commandes Vocales -->
        <service
            android:name=".voice.VoiceCommandService"
            android:foregroundServiceType="microphone"
            android:exported="false"
            android:stopWithTask="false" />

        <!-- Receiver démarrage automatique -->
        <receiver
            android:name=".hotword.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />  <!-- Pour certains appareils -->
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />  <!-- Pour certains appareils verrouillés -->
            </intent-filter>
        </receiver>

    </application>
</manifest>
